<Window x:Class="MonthlyClaimSystemPrototype.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin View" Height="500" Width="800">
    <Grid Margin="20">
        <DataGrid x:Name="dgClaims" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Claim ID" Binding="{Binding ClaimID}" Width="60"/>
                <DataGridTextColumn Header="Lecturer" Binding="{Binding LecturerName}" Width="120"/>
                <DataGridTextColumn Header="Hours" Binding="{Binding HoursWorked}" Width="80"/>
                <DataGridTextColumn Header="Rate" Binding="{Binding HourlyRate}" Width="80"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Document" Binding="{Binding DocumentPath}" Width="180"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,15,0,0">
            <Button x:Name="btnApprove" Content="Approve" Width="100" Margin="10" Click="btnApprove_Click"/>
            <Button x:Name="btnReject" Content="Reject" Width="100" Margin="10" Click="btnReject_Click"/>
        </StackPanel>
    </Grid>
</Window>
